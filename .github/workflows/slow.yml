name: slow

on:
  pull_request:
  workflow_dispatch:

jobs:
  slow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: extractions/setup-just@v2
      - run: just integration
      - run: just e2e
      - run: just ui-tests
